spring.application.name=Edu-Camp

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/tution_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123laki@*
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Change the Spring Boot server port
server.port=8080

# Session Configuration
server.servlet.session.timeout=1800s
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# Logging
logging.level.com.example.Edu_Camp=DEBUG
